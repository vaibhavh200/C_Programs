#include <stdio.h>
int main()
{
    float a,b;
    printf("Enter the side of the equi. triangle:");
    scanf("%f",&a);
    b=1.73/4*a*a;
    printf("The area the given equi. triangle is %g sq.units",b);
    return 0;
}
